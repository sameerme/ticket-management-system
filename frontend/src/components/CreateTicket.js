import React from "react";
import { Typography, Box } from "@mui/material";

const CreateTicket = () => {
    return (
        <Box sx={{ padding: 3 }}>
            <Typography variant="h4">Create Ticket</Typography>
            <Typography>Use this page to create a ticket.</Typography>
        </Box>
    );
};

export default CreateTicket;
